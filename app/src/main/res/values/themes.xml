<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base window theme. The real UI colours are produced at runtime by Compose /
        Material You (MaterialKolor), this theme only styles the very first frame and
        the system bars. Default is a dark surface so there is no white flash on launch.
    -->
    <style name="Theme.PRRCN" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
